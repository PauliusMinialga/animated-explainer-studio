{
  "name": "codeviz",
  "private": true,
  "scripts": {
    "dev": "cd client && npm install --legacy-peer-deps && npx vite",
    "build": "cd client && npm install --legacy-peer-deps && npx vite build",
    "preview": "cd client && npx vite preview"
  }
}
