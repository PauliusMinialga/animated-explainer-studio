# Root .env — read by both client (Vite) and server (FastAPI via pydantic-settings)
# Server-specific API keys go in server/.env (see server/.env.template)

# ── Client (Vite) ────────────────────────────────────────────────────────────
VITE_API_URL=http://localhost:8000
